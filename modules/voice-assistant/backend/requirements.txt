fastapi>=0.110
uvicorn[standard]>=0.27
pydantic-settings>=2.2
httpx>=0.27

# No STT backend here. Audio goes to text-whisperer, which owns the model,
# ffmpeg and the GPU -- see Documents/TEXT_WHISPERER_CONTRACT.md.
